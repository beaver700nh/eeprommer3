SOFTWARE:
  check for bugs
  update docs

FIRMWARE:
  quality-of-life:
    crashes e.g. file verify
    find and fix double screen clears
  fix page write (low priority):
    quick mode in eeprom.cpp - remove delay between bytes
    find way to optimize it
    remember to check 64-byte limit

HARDWARE:
  check if up to date

OTHER:
  update stuff like docs
