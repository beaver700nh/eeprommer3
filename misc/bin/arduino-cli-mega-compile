#!/bin/bash

prgm="$1"
shift
args=""

for arg in "$@"; do
  args="$args\"$arg\" "
done

if [[ "$args" == "" ]]; then
  args="[none]"
fi

echo -e "\x1b[1;96mCompiling sketch \"$prgm\" for Arduino Mega...\nFlags/arguments: $args\x1b[0m\n"
arduino-cli compile --fqbn arduino:avr:mega "$@" "$prgm"
