Put these three executables into a system executable directory such as
/usr/local/bin/. Could be any directory so long as it is in the PATH
environment variable. Also make sure to make the files executable.
Note that you will have to install arduino-cli yourself if your system
is not Linux ARM-32.
